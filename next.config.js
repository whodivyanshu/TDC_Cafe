/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.icons8.com", "firebasestorage.googleapis.com", "images.unsplash.com", "plus.unsplash.com"]
    }
}

module.exports = nextConfig
